{
  "scripts": {
    "dev": "bash scripts/dev.sh",
    "build": "npm run check:csp && astro build",
    "preview": "astro preview",
    "check:csp": "node scripts/check-csp.mjs"
  },
  "engines": {
    "node": "22.x"
  },
  "dependencies": {
    "@astrojs/mdx": "^6.0.2",
    "astro": "^6.4.4"
  },
  "optionalDependencies": {
    "defu": "^6.1.5"
  },
  "overrides": {
    "esbuild": "^0.28.1"
  }
}
